@{
    ViewData["Title"] = "Home Page";
}

@{  if(Context.Request.Cookies.ContainsKey("cookie"))
    {
        <div class="text-center">
            <h2>Welcome!</h2>
        </div>
    }
    else
    {
        <div class="text-center">
            <button type="button" onclick="location.href='@Url.Action("Login", "Auth")'">Login With gitlab</button>
        </div>
    }
}